-if posts.present?
  -posts.each do |post|
    =render partial: "front/posts/preview", locals: { post: post }
  -if Post.count > Pagy::VARS[:items]
    .pagy_nav
      != pagy_bootstrap_nav(@pagy)

-else
  %h4="No posts found for '#{params[:query]}'. Please try a new search."
